# Tests

These tests are public-safe smoke checks for the portfolio snapshot.

They do not require Deadline, MongoDB, NAS, Maya, Blender, or internal lab infrastructure.

The goal is to verify:

- expected source snapshot paths exist
- public documentation exists
- placeholder configuration exists
- core Python files can be parsed by Python
